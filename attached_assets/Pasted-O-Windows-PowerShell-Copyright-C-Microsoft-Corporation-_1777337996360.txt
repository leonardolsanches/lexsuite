O Windows PowerShell
Copyright (C) Microsoft Corporation. Todos os direitos reservados.

Instale o PowerShell mais recente para obter novos recursos e aprimoramentos! https://aka.ms/PSWindows

PS C:\WINDOWS\system32> ssh leonardo@192.168.0.38
leonardo@192.168.0.38's password:
Welcome to Ubuntu 24.04.4 LTS (GNU/Linux 6.8.0-110-generic x86_64)

 * Documentation:  https://help.ubuntu.com
 * Management:     https://landscape.canonical.com
 * Support:        https://ubuntu.com/pro

 System information as of ter 28 abr 2026 00:57:49 UTC

  System load:           0.07
  Usage of /:            15.9% of 491.58GB
  Memory usage:          2%
  Swap usage:            0%
  Temperature:           58.0 C
  Processes:             303
  Users logged in:       1
  IPv4 address for wlo1: 192.168.0.38
  IPv6 address for wlo1: 2804:14d:7886:81c6:ee4c:8cff:fe21:acdc

 * Strictly confined Kubernetes makes edge and IoT secure. Learn how MicroK8s
   just raised the bar for easy, resilient and secure K8s cluster deployment.

   https://ubuntu.com/engage/secure-kubernetes-at-the-edge

Manutenção de Segurança Expandida para Applications não está ativa.

54 as atualizações podem ser aplicadas imediatamente.
6 dessas atualizações são atualizações de segurança padrão.
Para ver as actualizações adicionais corre o comando: apt list --upgradable

13 atualizações de segurança adicionais podem ser aplicadas com ESM Apps.
Saiba mais sobre como ativar o serviço ESM Apps at https://ubuntu.com/esm


Last login: Tue Apr 28 00:55:38 2026 from 192.168.0.34
leonardo@agile-ai-experts-server:~$ cat > ~/start-tunnels.sh << 'EOF'
#!/bin/bash
# Mata túneis anteriores
pkill -f "cloudflared tunnel" 2>/dev/null
sleep 2

RENDER_API_KEY="sua-render-api-key"
LEX_API_SERVICE_ID="seu-service-id-lex-api"
LEX_SUITE_SERVICE_ID="seu-service-id-lexsuite"

# Sobe túnel Ollama e captura URL
cloudflared tunnel --url http://localhost:11434 2>&1 | \
  tee /tmp/ollama-tunnel.log &

# Sobe túnel DB Bridge e captura URL
cloudflared tunnel --url http://localhost:8000 2>&1 | \
  tee /tmp/bridge-tunnel.log &

# Aguarda as URLs aparecerem nos logs
sleep 10

OLLAMA_URL=$(grep -o 'https://[^ ]*\.trycloudflare\.com' /tmp/ollama-tunnel.log | head -1)
BRIDGE_URL=$(grep -o 'https://[^ ]*\.trycloudflare\.com' /tmp/bridge-tunnel.log | head -1)

echo "Ollama: $OLLAMA_URL"
echo "Bridge: $BRIDGE_URL"

# Atualiza o Render automaticamente via API
curl -s -X PUT "https://api.render.com/v1/services/$LEX_API_SERVICE_ID/env-vars" \
  -H "Authorization: Bearer $RENDER_API_KEY" \
chmod +x ~/start-tunnels.shSE_URL\",\"value\":\"$OLLAMA_URL\"}]"RVICE_ID/env-vars" \URL\",\"value\":\"$BRIDGE_URL\"}]"
leonardo@agile-ai-experts-server:~$ sudo tee /etc/systemd/system/lex-tunnels.service << 'EOF'
[Unit]
Description=Lex Suite Cloudflare Tunnels
After=network-online.target ollama.service
Wants=network-online.target

[Service]
User=leonardo
ExecStart=/home/leonardo/start-tunnels.sh
Restart=on-failure
RestartSec=30

[Install]
WantedBy=multi-user.target
EOF

sudo systemctl daemon-reload
sudo systemctl enable lex-tunnels
sudo systemctl start lex-tunnels
[sudo] password for leonardo:
[Unit]
Description=Lex Suite Cloudflare Tunnels
After=network-online.target ollama.service
Wants=network-online.target

[Service]
User=leonardo
ExecStart=/home/leonardo/start-tunnels.sh
Restart=on-failure
RestartSec=30

[Install]
WantedBy=multi-user.target
Created symlink /etc/systemd/system/multi-user.target.wants/lex-tunnels.service → /etc/systemd/system/lex-tunnels.service.
leonardo@agile-ai-experts-server:~$